version: "2"

options:
  verbose: true
syncs:
  VOLUME_NAME: # tip: add -sync and you keep consistent names as a convention
    src: './'
    sync_strategy: 'unison'
      #sync_excludes: ['ignored_folder', '.ignored_dot_folder']
